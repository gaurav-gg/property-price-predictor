df2 <- select(df2,rlat,rlon)
df2 <- unique(df2)
old <- read.csv("Data/propertywiseamen.csv")
old <- select(old,lat,lon)
names(old) <- c("rlat","rlon")
change <- setdiff(df2,old)
change1 <- read.csv("change.csv")
change2 <- setdiff(change1,change)
change1 <- change1[(1:15300),] %>% as.data.frame()
change1 <- rbind(change1,change2)
write.csv(change1,"change.csv",row.names = F)
